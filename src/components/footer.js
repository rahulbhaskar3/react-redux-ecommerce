import React from 'react';
import ReactDom from 'react-dom';

const footer = (props) => {
    return(
        <div className="App">
            <h1>This is the footer</h1>
        </div>
    );
}

export default footer;